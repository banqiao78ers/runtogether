---
description: 板橋約跑 PWA 專案環境與技術基準
alwaysApply: true
---

# 板橋約跑 PWA — 專案環境

## 權威規格
- 唯一技術基準：`banqiao_running_pwa_spec.md`
- Schema migration：`supabase/migrations/001_pwa_schema.sql`

## 遠端與基礎設施
- **GitHub**：https://github.com/banqiao78ers/runtogether.git
- **共用 Supabase**：https://epmfustvktuqwppwgezt.supabase.co
- 新建表／RPC 一律 `pwa_` 前綴；可唯讀 `profiles` 供後台對照，禁止改既有表結構

## 舊會員規則 C
- `profiles` 無 `line_uid`；首次 LINE 登入一律 `member`
- Admin 於 `/admin/users` 對照 `profiles.display_name` 手動升格 `super_member`／`admin`

## 技術棧邊界
- Next.js App Router + PWA，部署於 Vercel（Cron：`/api/cron/tick`，Hobby 為每日一次）
- LINE Login + HttpOnly Cookie Session（30 天滑動）
- Web Push：獨立 VAPID、單一裝置覆蓋、Fire-and-forget
