# Supabase（與既有系統共用）
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# App
NEXT_PUBLIC_APP_URL=
# LINE Login（同 Provider 下獨立 Channel）
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=
LINE_CALLBACK_URL=

# Session（至少 16 字元）
SESSION_SECRET=

# Web Push（獨立 VAPID，勿與其他專案混用）
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=

# 定時任務（cron-job.org 等外部排程呼叫 /api/cron/tick）
# 正式環境必填；本機未設時 dev 可免驗證
CRON_SECRET=
